services:
  backend:
    build: ./backend
    container_name: marketmax-backend
    ports:
      - "9000:9000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: marketmax-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend